# Study Buddy Finder - System Architecture & Data Flow

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT BROWSER                           │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ index.html   │  │ home.html    │  │ profile.html │          │
│  │ (Login)      │  │ (Listings)   │  │ (Profile)    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                   │                  │
│         └─────────────┬───┴───────────────────┘                  │
│                       │                                           │
│         ┌─────────────▼─────────────┐                            │
│         │   JavaScript (Client)     │                            │
│         │                           │                            │
│         │ • index.js (login)        │                            │
│         │ • home.js (listings)      │                            │
│         │ • profile.js (profile)    │                            │
│         │                           │                            │
│         │ API Base:                 │                            │
│         │ "http://localhost:3000"   │                            │
│         └────────────┬──────────────┘                            │
│                      │                                            │
│    fetch() with credentials: 'include'  (httpOnly cookies)       │
└──────────────────────┼──────────────────────────────────────────┘
                       │
                       │ HTTPS/Cookie
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│                   EXPRESS SERVER (Node.js)                        │
│                     server.js                                     │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ AUTHENTICATION ENDPOINTS                                  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ POST   /signup       → supabase.auth.signUp()             │ │
│  │ POST   /login        → supabase.auth.signInWithPassword() │ │
│  │ GET    /me           → Verify session, return user        │ │
│  │ POST   /logout       → Clear auth cookie                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ LISTING CRUD ENDPOINTS (Require Auth)                     │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ GET    /listings     → supabase.from('listings').select() │ │
│  │ POST   /listings     → Create new listing                 │ │
│  │ PUT    /listings/:id → Update listing                     │ │
│  │ DELETE /listings/:id → Delete listing                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  Session: httpOnly Cookie (Supabase Session)                     │
│  CORS: Configured for localhost                                  │
│  Body Parser: JSON support                                       │
│  Error Handling: Try/catch on all endpoints                      │
└──────────────────────────┬─────────────────────────────────────┘
                           │
                           │ SQL Queries
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│              SUPABASE POSTGRESQL DATABASE                         │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ AUTH.USERS (Supabase Auth)                                │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ id                UUID (primary key)                       │ │
│  │ email             TEXT (unique)                            │ │
│  │ encrypted_password TEXT (bcrypt hashed)                    │ │
│  │ created_at        TIMESTAMP                                │ │
│  │ ...               (other Supabase fields)                 │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ LISTINGS (Application Data)                               │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ id          UUID (primary key)                            │ │
│  │ user_id     UUID (FK → auth.users.id)                     │ │
│  │ location    TEXT                                           │ │
│  │ group_size  INTEGER                                        │ │
│  │ time        TEXT                                           │ │
│  │ description TEXT                                           │ │
│  │ user_email  TEXT (denormalized for display)               │ │
│  │ created_at  TIMESTAMP                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ROW LEVEL SECURITY (RLS) POLICIES                          │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ SELECT: User can only see their own listings              │ │
│  │ INSERT: New listings auto-assigned to current user         │ │
│  │ UPDATE: User can only update their own listings            │ │
│  │ DELETE: User can only delete their own listings            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  Backups: Automatic (Supabase)                                   │
│  Scaling: Automatic (PostgreSQL)                                 │
│  Monitoring: Supabase Dashboard                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Authentication Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    SIGNUP PROCESS                                │
└─────────────────────────────────────────────────────────────────┘

USER ENTERS EMAIL/PASSWORD
         │
         ▼
┌────────────────────────┐
│ signup.html form       │
│ Validates length       │
└────────────┬───────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ fetch("POST /signup", {                        │
│   body: { email, password }                    │
│   credentials: 'include'                       │
│ })                                             │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Express Server /signup endpoint                │
│ 1. Parse email & password from request         │
│ 2. Call supabase.auth.signUp({...})            │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Supabase Auth                                  │
│ 1. Hash password with bcrypt                   │
│ 2. Store in auth.users table                   │
│ 3. Return session & tokens                     │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Express Server                                 │
│ 1. Receive session from Supabase               │
│ 2. setAuthCookies() - Set httpOnly cookie      │
│ 3. Response: { user: {...} }                   │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Browser                                        │
│ 1. Cookie set (automatic, JavaScript can't    │
│    access due to httpOnly flag)                │
│ 2. Redirect to home.html                       │
└────────────┬───────────────────────────────────┘
             │
             ▼
AUTHENTICATED & LOGGED IN ✅


┌─────────────────────────────────────────────────────────────────┐
│                    SUBSEQUENT REQUEST                            │
└─────────────────────────────────────────────────────────────────┘

USER CLICKS "CREATE LISTING"
         │
         ▼
┌────────────────────────┐
│ home.js form           │
│ Validates inputs       │
└────────────┬───────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ fetch("POST /listings", {                      │
│   body: { location, group_size, ... }          │
│   credentials: 'include'  ← Cookie sent here   │
│ })                                             │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Browser Automatically Includes Cookie          │
│ (httpOnly, so JavaScript doesn't see it)       │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Express Server /listings endpoint              │
│ 1. Extract cookie from request                 │
│ 2. Verify session with supabaseAuthed(req)     │
│ 3. Get user ID from verified session           │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Supabase PostgreSQL Insert                     │
│ INSERT INTO listings (                         │
│   user_id,          ← From verified session    │
│   location,         ← From request             │
│   group_size,       ← From request             │
│   time,             ← From request             │
│   description,      ← From request             │
│   user_email,       ← From user data           │
│   created_at        ← Current timestamp        │
│ )                                              │
│                                                │
│ RLS Policy Enforces:                           │
│ - user_id matches authenticated user           │
│ - Cannot insert for other users                │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Express Server                                 │
│ 1. Receive success from database               │
│ 2. Return { success: true, listing: {...} }    │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ home.js                                        │
│ 1. Display success message                     │
│ 2. Clear form                                  │
│ 3. Call displayListings()                      │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ fetch("GET /listings", {                       │
│   credentials: 'include'  ← Cookie sent        │
│ })                                             │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Express Server                                 │
│ 1. Verify session                              │
│ 2. Get user ID from session                    │
│ 3. Query Supabase for user's listings          │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Supabase PostgreSQL Query                      │
│ SELECT * FROM listings                         │
│ WHERE user_id = authenticated_user_id          │
│                                                │
│ RLS Policy Enforces:                           │
│ - Database only returns user's own listings    │
│ - Even if query doesn't filter, RLS enforces   │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ Express Server                                 │
│ Return: [ { id, location, group_size, ... } ] │
└────────────┬───────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│ home.js displayListings()                      │
│ Loop through listings                          │
│ Create HTML elements                           │
│ Append to page                                 │
└────────────┬───────────────────────────────────┘
             │
             ▼
USER SEES THEIR LISTINGS ✅
```

---

## 🔐 Security Layers Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                    SECURITY ARCHITECTURE                          │
└──────────────────────────────────────────────────────────────────┘

LAYER 1: PASSWORD SECURITY
─────────────────────────
User inputs password in form
         │
         ▼
Transmitted via HTTPS
         │
         ▼
Server receives password
         │
         ▼
Passed to Supabase Auth
         │
         ▼
Supabase hashes with bcrypt (not reversible!)
         │
         ▼
Hashed password stored in database
         │
         ▼
Original password never stored ✅


LAYER 2: SESSION SECURITY
──────────────────────────
User logs in
         │
         ▼
Supabase creates JWT token
         │
         ▼
Express stores token in httpOnly cookie
         │
         ▼
JavaScript cannot access (httpOnly flag!)
         │
         ▼
Browser automatically sends with requests
         │
         ▼
Server verifies token before processing
         │
         ▼
Token expires after time limit
         │
         ▼
Session is secure from JavaScript attacks ✅


LAYER 3: DATABASE SECURITY (RLS)
─────────────────────────────────
User requests their listings
         │
         ▼
Express gets user ID from verified session
         │
         ▼
Query sent to PostgreSQL
         │
         ▼
RLS Policy intercepts:
  IF user_id in query ≠ authenticated_user_id
    THEN deny access (no results returned)
    ELSE allow (return user's data)
         │
         ▼
Database enforces permission
         │
         ▼
User cannot access others' data ✅


LAYER 4: DATA VALIDATION
────────────────────────
User submits form
         │
         ▼
Frontend validates (email format, password length)
         │
         ▼
Server re-validates on receipt
         │
         ▼
Prevents SQL injection, XSS, etc.
         │
         ▼
Safe data stored ✅


LAYER 5: TRANSPORT SECURITY
────────────────────────────
Browser sends request
         │
         ▼
HTTPS/TLS encrypts transmission
         │
         ▼
Man-in-the-middle cannot see data
         │
         ▼
Server receives encrypted data
         │
         ▼
Secure communication ✅
```

---

## 📊 Data Flow - Complete User Journey

```
NEW USER SIGNUP → LOGIN → CREATE LISTING → VIEW LISTING → LOGOUT

┌──────────────┐
│ User visits  │
│ index.html   │
└──────┬───────┘
       │
       ▼
   Signup form?
       │
       ├─ YES ─┐
       │       │
       │       ▼
       │   ┌─────────────────┐
       │   │ signup.html     │
       │   │ - Form          │
       │   │ - Validation    │
       │   └────────┬────────┘
       │            │
       │            ▼
       │   POST /signup
       │       ↓
       │   Supabase creates user
       │       ↓
       │   httpOnly cookie set
       │       ↓
       │   Redirect home.html
       │            │
       │            └──┐
       │               │
       ▼               │
   ┌────────────────┐  │
   │ index.html     │  │
   │ (Login form)   │◄─┘
   └────────┬───────┘
            │
            ▼
         Email/Password
            │
            ▼
         POST /login
            │
            ▼
         Supabase authenticates
            │
            ▼
         httpOnly cookie set
            │
            ▼
         Redirect home.html
            │
            ▼
      ┌──────────────┐
      │ home.html    │
      │ (Listings)   │
      │              │
      │ GET /me      │ (Verify session)
      │   ↓          │
      │ checkAuth()  │
      │   ↓          │
      │ verified ✅  │
      │              │
      │ GET /listings│ (Load listings)
      │   ↓          │
      │ displayListings()
      │   ↓          │
      │ Show form    │
      └──────┬───────┘
             │
             ▼
      User fills form
             │
             ▼
      POST /listings
             │
             ▼
      Express validates:
      - User authenticated ✅
      - User ID extracted ✅
      - Data validated ✅
             │
             ▼
      PostgreSQL INSERT
      - RLS enforces user_id ✅
      - Data persisted ✅
             │
             ▼
      Response: success
             │
             ▼
      JavaScript:
      - Clear form
      - Refresh listings
             │
             ▼
      GET /listings
             │
             ▼
      Display updated list
             │
             ▼
      User sees their listing ✅
             │
             ▼
      Click "Profile"
             │
             ▼
      ┌──────────────────┐
      │ profile.html     │
      │ (User profile)   │
      │                  │
      │ GET /me          │ (Load profile)
      │ GET /listings    │ (Load their listings)
      │                  │
      │ Can EDIT/DELETE  │
      │ - Edit: PUT      │
      │ - Delete: DELETE │
      │ - RLS enforces   │
      └────────┬─────────┘
               │
               ▼
            User can:
            - View profile
            - Edit listings
            - Delete listings
               │
               ▼
           Click "Sign Out"
               │
               ▼
           POST /logout
               │
               ▼
           Cookie cleared
               │
               ▼
           Redirect index.html
               │
               ▼
           Session ended ✅
```

---

## 🗄️ Database Schema

```
SUPABASE PROJECT
├── auth.users (Supabase Auth)
│   └── Managed by Supabase
│       ├── id (UUID)
│       ├── email
│       ├── encrypted_password (bcrypt)
│       ├── created_at
│       └── ... (other Supabase fields)
│
└── public.listings (Application Data)
    ├── Columns:
    │   ├── id (UUID, PRIMARY KEY)
    │   ├── user_id (UUID, FK → auth.users.id)
    │   ├── location (TEXT)
    │   ├── group_size (INTEGER)
    │   ├── time (TEXT)
    │   ├── description (TEXT)
    │   ├── user_email (TEXT)
    │   └── created_at (TIMESTAMP)
    │
    ├── Indexes:
    │   ├── PRIMARY KEY (id)
    │   ├── FK (user_id)
    │   └── INDEX (user_id) for fast user queries
    │
    └── RLS Policies:
        ├── SELECT: WHERE user_id = auth.uid()
        ├── INSERT: Assign to auth.uid()
        ├── UPDATE: WHERE user_id = auth.uid()
        └── DELETE: WHERE user_id = auth.uid()
```

---

## 🌐 Request/Response Examples

### Example 1: Signup
```
REQUEST:
POST http://localhost:3000/signup
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}

RESPONSE:
201 Created
Set-Cookie: sb-auth-token=...; httpOnly; sameSite=lax

{
  "user": {
    "id": "uuid-here",
    "email": "user@example.com",
    "created_at": "2024-01-01T12:00:00Z"
  }
}
```

### Example 2: Create Listing
```
REQUEST:
POST http://localhost:3000/listings
Content-Type: application/json
Cookie: sb-auth-token=...

{
  "location": "Library",
  "group_size": 3,
  "time": "2:00 PM",
  "description": "Math study session"
}

RESPONSE:
201 Created

{
  "id": "uuid-here",
  "user_id": "user-uuid",
  "location": "Library",
  "group_size": 3,
  "time": "2:00 PM",
  "description": "Math study session",
  "user_email": "user@example.com",
  "created_at": "2024-01-01T12:00:00Z"
}
```

### Example 3: Get Listings
```
REQUEST:
GET http://localhost:3000/listings
Cookie: sb-auth-token=...

RESPONSE:
200 OK

[
  {
    "id": "uuid-here",
    "user_id": "user-uuid",
    "location": "Library",
    "group_size": 3,
    "time": "2:00 PM",
    "description": "Math study session",
    "user_email": "user@example.com",
    "created_at": "2024-01-01T12:00:00Z"
  }
]

(Only shows listings where user_id matches authenticated user - RLS enforced)
```

---

**Diagram Complete!** This shows the complete architecture, flows, and security layers of Study Buddy Finder.
